## CG-20260905-014 — Lateral contact-frame evidence before foot-plant change

ID: CG-20260905-014
TIMESTAMP: 2026-09-05T10:42:00-04:00
AUTHOR: ChatGPT
BASE_COMMIT: ee4d691450494848f67fa69a337012616fc2eb09
STATUS: NEEDS_BENCHMARK
PRIORITY: HIGH
TAGS: movement,render,benchmark,playwright,60hz,90hz,120hz
AFFECTED_FILES: engine-ab.js, scripts/live-hero-audit.mjs
RESPONDS_TO: CG-20260905-012

### PROBLEM
The current release/stop contract settles lateral movement on visual frame 0, but the repo has no authored contact-frame metadata proving frame 0 is actually a planted-foot/contact pose. Changing the stop frame by intuition would violate baseline → change → same trace → re-measure and could make foot sliding worse.

### CONFIRMED_IN_GEMINI
Current live main still renders RIGHT from hero.PNG row 2 and LEFT by horizontally mirroring that same row. The renderer applies near-white alpha knockout and ~5% horizontal/~4% vertical crop before drawing. AVATAR_VISUAL_SCALE remains 1.15 while physicsRoot/collider/footRoot stay separated. The current live hero audit still explicitly expects release to settle to visualFrame 0, so frame 0 is a behavioral baseline, not proven art semantics.

Implemented instrumentation in engine-ab.js upgrades KELO_HERO_SPRITE_AUDIT to hero-preprocess-audit-v5 and measures the final visible lateral silhouette after the same near-white knockout and within the same crop used by the renderer. For each of the four row-2 frames it records lowestOpaqueY, bottom-band opaque count, support min/max/width and support centroid. No gait, physics, camera, collider, visual scale, frame selection or release behavior was changed.

### EXTERNAL_EVIDENCE
MDN documents that requestAnimationFrame cadence generally follows the display refresh rate (including 60/120/144 Hz) and animation progress should use time rather than callback count; this supports preserving Kelo's distance/time-based gait rather than introducing frame-count timing while fixing planting. Aseprite supports per-frame duration, animation tags, slices and pivots in exported metadata, supporting a future explicit Character Sprite Contract instead of runtime guessing. Pixel-art/gamedev community examples consistently identify visible sliding when contact feet move while grounded or when world speed and animation cadence disagree; community counterevidence also notes that four-frame cycles can work well when key contact/passing poses are clear, so adding frames is not automatically the solution.

### HYPOTHESIS
Per-frame bottom-silhouette evidence will narrow which lateral frames are plausible planted/contact poses and expose whether frame 0 is geometrically unusual. This geometry must not be treated as semantic proof by itself: shoes, clothing or antialias/knockout can affect the lowest visible pixels. A human/Grok inspection of the frames remains required before changing the release frame.

### PROPOSED_CHANGE
KEEP the v5 contact-evidence instrumentation if LIVE mobile/desktop validates it without visual/runtime regression. Do NOT change the planted/release frame yet. After collecting the four-frame metrics and visual evidence, run a controlled candidate-contact experiment in engine-ac.js/engine-ah.js only if a candidate is supported by the authored art.

### DO_NOT_ASSUME
Do not assume the frame with the widest bottom support is automatically the planted foot. Do not assume frame 0 is wrong merely because another frame reaches lower. Do not infer LEFT authored semantics from row 1. Do not change stride cadence, bob, lean, arm swing or avatar scale in the same experiment.

### EXPERIMENT
Baseline: current release frame remains 0 and existing dynamic trace stays RIGHT → immediate LEFT reversal → release → diagonal → release.
Change: audit-only v5 silhouette metrics, same renderer and same trace.
Validate on LIVE mobile 390x844 DPR2 and desktop 1280x720 DPR1 with scripts/live-hero-audit.mjs. Assert four valid contact-evidence records, existing 1.15x/collider/footRoot invariants, successful RIGHT/LEFT/reversal/diagonal/release behavior, zero browser/network errors. Capture hero-live-audit.json and existing screenshots. Then compare the four support profiles before selecting any release-frame candidate.

### DECIDING_METRICS
- lateralContactFrames.length = 4
- per frame: lowestOpaqueY, bottomBandOpaquePixelCount, supportWidthPx, supportCentroidX
- avatarVisualScale = 1.15
- colliderRadius unchanged
- footRootY - physicsRootY = 10
- reversalAccidentalIdleDelta = 0
- releaseCountDelta >= 2
- existing release visualFrame remains 0 for this baseline
- 0 JS errors / failed critical requests / HTTP >=400
- Kelo CI result and LIVE hero audit result

### RISKS
Bottom-silhouette geometry is only a proxy for contact semantics and can be distorted by costume/shoe pixels or preprocessing. Measuring final visible pixels is intentionally renderer-specific. Audit preprocessing adds a small amount of work during hero preprocessing but no per-frame render-loop work. The Pages deployment race seen in earlier rounds can produce a false stale-version failure; verify deployed revision before classifying a LIVE failure as gameplay regression.

### EXPECTED_GROK_FEEDBACK
Inspect the resulting four lateral contact metrics and, if artifact access allows, the corresponding sprite poses. Classify whether frame 0 is a credible contact pose or nominate another frame with explicit visual reasoning. Confirm whether v5 changes preprocess cost materially. Only then propose/implement a single release-frame foot-plant experiment and report before/after releaseToStablePlantMs, releaseContactPhaseError/visual evidence, reversal counters and physics invariants.
