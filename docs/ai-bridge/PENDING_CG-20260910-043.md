# CG-20260910-043 — Light-basic active movement freedom A/B

- **Date:** 2026-09-10
- **Status:** IMPLEMENTED_VERIFIED
- **Area:** PvP / melee movement feel / server parity
- **Owner touched:** `KeloMeleeProfiles` (`src/systems/melee/melee-weapon-profiles.js`)
- **Reason for pending file:** safe append mechanism; canonical bridge history was not replaced.

## Problem

The sword light-basic opener still used `movementScale.active = 0.48`, making the 75 ms active window slightly more restrictive than the light follow (`0.52`). With previous recovery mobility improvements already landed, this active-phase mismatch was the next low-risk control gap in the light combo.

## Baseline

`sword_light_basic` before this round:
- windup movementScale `0.86`
- active movementScale `0.48`
- recovery movementScale `1.00`
- timings unchanged: `85 / 75 / 180 ms`
- damage/range/hit geometry/knockback/cooldown unchanged.

At reference speed 185.28 px/s, continuous whole-action displacement was ~53.564 px and weighted movement freedom ~85.03%.

## Candidate A — 0.52

Changed only light-basic active movementScale `0.48 -> 0.52` through the existing shared `KeloMeleeProfiles` owner. The server already loads this profile and resolves phase movement through the shared `KeloMeleeEngine.movementScaleFor`, preserving client/server semantics.

Real Chromium Strict PvP Quality Judge run `34487518099` passed. Mobile evidence included:
- touch latency: 41.3 ms
- attackMovementRatio: 0.796
- dodge displacement: 120.46 px
- knockback displacement: 60.07 px
- frame P95: 16.7 ms
- page errors: 0
- combo order remained basic -> follow -> finisher.

Candidate A improved continuous whole-action displacement by ~0.556 px and weighted movement freedom to ~85.91%.

## Error found after Candidate A

`Melee Recovery Mobility CI` initially failed because its existing audit still hard-coded the obsolete basic active scale `0.48`. This was a stale contract, not a gameplay regression. The existing audit was updated rather than creating a duplicate harness.

## Candidate B — 0.55

As the one permitted low-risk refinement, active movementScale was tested at `0.55`.

Real Chromium Strict PvP Quality Judge run `34488352228` also passed and raised `attackMovementRatio` to ~0.809. However, the whole-action gain over Candidate A was only ~0.417 px, while the opener became freer during active frames than the follow attack (`0.52`). Agent B classified this as EMPATA in raw control but PIERDE overall because it weakens light-combo weight progression for a marginal gain.

## Winner

**Candidate A: active movementScale = 0.52 — GANA.**

It keeps the opener and follow at the same active movement restriction while measurably improving control over the old `0.48`. Candidate B `0.55` was not retained.

Final shared profile contract:
- basic: `.86 / .52 / 1.00`
- follow active/recovery: `.52 / 1.00`
- finisher recovery: `.76`
- heavy recovery: `.64`

## 60 / 90 / 120 Hz deterministic evidence

The existing `scripts/melee-recovery-mobility-audit.js` now executes baseline `0.48`, winner `0.52`, and refinement `0.55` at 60/90/120 Hz, locks the winner, verifies opener/follow active hierarchy, and verifies server use of the shared profile/timeline.

Approximate whole-action traces at 185.28 px/s:
- 60 Hz: 53.688 -> 54.367 px winner; 0.55 refinement ~54.877 px
- 90 Hz: 53.717 -> 54.313 px winner; 0.55 refinement ~54.761 px
- 120 Hz: 53.611 -> 54.228 px winner; 0.55 refinement ~54.692 px

## Runtime / CI evidence

After the winner was restored, broad gates exposed two additional stale test assumptions:
1. `pvp-action-combat-audit.js` still searched for the ancient scalar literal `movementScale:0.72` rather than phase movement data.
2. `pvp-action-visual-audit.mjs` waited for lazy-loaded abilities before invoking the runtime bootstrap.

Both existing harnesses were repaired without changing gameplay. Final PvP Action Combat CI run `34489156642` passed both jobs:
- deterministic contracts: SUCCESS
- Chromium visual-mobile-desktop: SUCCESS

The browser visual harness still exercises mobile 390x844 touch and desktop 1440x900, including sword windup/active/recovery, Fireball presentation and Wind Dash, and rejects hard page errors.

## Commits

- Candidate A 0.52: `e2c6d2f749380fe44a8899ae0d9ecd09f4d7541a`
- stale melee mobility audit repair: `044794941db952339b5f7a89b7aff5019bd6c231`
- Candidate B 0.55: `77718f8b0dd228e4fd254140c9bf5ee6437fad86`
- winner restored at 0.52: `66f61dc163e7d3a82e08630d996e6b06cd818a94`
- 60/90/120 winner lock: `ac9a97627ad845488757131e677fb656ab6edfd6`
- PvP action contract refresh: `c1c63f6ada037fd260a10bc6c5ae7fe62c995b61`
- lazy-runtime visual harness repair: `456e55540505974df79de8acc8a4ce5f810112cf`

## Agent B verdict

**GANA — Candidate A 0.52.**

Primary perceptual improvement: the light opener retains more directional control during the contact window without erasing attack weight or making it freer than its follow-up. No evidence of new input lag, aim regression, dodge regression, knockback regression, page errors, or frame-time regression in the executed judge.

## Next gap

Next round should execute the combined interaction **45-degree movement + independent/opposed aim + basic attack**, measuring movement-vector preservation, facing-row continuity and camera framing during windup/active/recovery on mobile and desktop, rather than retuning this movementScale again without new evidence.
