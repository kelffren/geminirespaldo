# CG-20260910-046 — Cast → Recovery → Dodge Camera Release

- **Direction:** ChatGPT → Grok
- **Author:** ChatGPT
- **Date:** 2026-09-10
- **Repo:** `kelffren/gemini`
- **Branch:** `main`
- **Status:** WINNER VALIDATED IN MAIN
- **Area:** PvP / Camera / Combat Feel
- **Owner touched:** `KeloCamera` (`src/core/camera-system.js`)
- **Architecture:** no new manager/engine/wrapper; presentation-only camera change; gameplay, collision, ability authority and server contracts unchanged.

## Gap

Stationary Fireball cast builds screen-space aim framing toward the target during `windup/active`. When the player enters `recovery` and immediately dodges, the old aim framing previously decayed with the generic action decay (`9`), so the camera kept approximately 15.7 px of stale target-side composition into almost the entire 160 ms dodge.

## Baseline — same Chromium trace

Scenario: stationary local player, aim RIGHT, real Fireball cast via `KeloAbilities` bus, wait for `active`, then `recovery`, push real `DODGE_PRESS`, sample `KeloCamera.snapshot().combatFraming.stationaryActionOffsetScreenX` through the 112 px dodge.

- Mobile landscape 844×390 DPR2/touch: before-dodge carry ≈15.66 px; settle to ≤4 px ≈150.8 ms; dodge travel 112 px; no page errors.
- Desktop 1440×900: before-dodge carry ≈15.67 px; settle to ≤4 px ≈165.1 ms; dodge travel 112 px; no page errors.
- Perceptual defect: old cast aim composition remained visible for nearly the whole dodge and resisted the new mobility direction.

## Candidate A

Added a context-specific release decay only while `dodgeActive` and stationary action aim is no longer committed:

- generic action framing decay: 9 (unchanged)
- Candidate A dodge release decay: 18

Deterministic 60/90/120 Hz improved to 83.33 / 77.78 / 83.33 ms, with max per-step changes 4.07 / 2.85 / 2.19 px.

### Agent B verdict — EMPATA / NOT ACCEPTED

Candidate A materially improved control and did not snap, but the real mobile Chromium gate measured **90.3 ms**, narrowly above the predefined ≤90 ms acceptance boundary. The candidate was not accepted as winner; it was immediately refined.

## Candidate B — WINNER

Refined only the dodge-specific camera release:

- `PVP_ACTION_DODGE_RELEASE_DECAY: 18 → 22`
- acquisition/general action framing decay remains 9
- stationary screen lead remains 28 px
- dead-zone, look-ahead, movement, dodge distance/timing, collision, damage, cast timings and networking unchanged

Gameplay commit: `2dbdaa30185a2e01f0b6cac041af762b8ec6cf0d`

Deterministic A/B commit / winner lock: `a0b23bf01e0293dace516a7b4158ce25016195e2`

## Winner metrics

### Deterministic

- 60 Hz: settle 66.67 ms; max step 4.819 px
- 90 Hz: settle 66.67 ms; max step 3.405 px
- 120 Hz: settle 66.67 ms; max step 2.630 px

### Chromium exact checkout

Mobile landscape 844×390 DPR2/touch:
- cast framing peak: 18.196 px
- before dodge: 15.643 px, phase `recovery`
- settle ≤4 px: **73.8 ms**
- max release step: **4.142 px**
- dodge travel: **112 px**
- page errors: **0**

Desktop 1440×900:
- cast framing peak: 18.205 px
- before dodge: 15.664 px, phase `recovery`
- settle ≤4 px: **71.1 ms**
- max release step: **4.124 px**
- dodge travel: **112 px**
- page errors: **0**

Strict workflow: `PvP Cast Dodge Camera CI` run `34503700782` — SUCCESS.
Kelo CI run `34503700683` — SUCCESS.
Foundation Architecture CI run `34503700770` — SUCCESS.

## Agent B final verdict — GANA

Candidate B releases stale cast framing in roughly the first half of the dodge instead of carrying it through nearly the whole dodge, while remaining well below the 8 px continuity/snap guard. It preserves target legibility during the committed cast and returns visual priority to mobility once dodge begins.

## Evidence / reusable guards

- `scripts/live-pvp-cast-dodge-camera-judge.mjs`
- `scripts/audit-pvp-cast-dodge-camera-timing.mjs`
- `.github/workflows/pvp-cast-dodge-camera-ci.yml`

The browser judge reproduces mobile landscape + desktop from the same real action sequence. The timing audit locks baseline/A/B behavior at 60/90/120 Hz.

## Remaining gap / next target

The dodge transition is now covered. The next camera composition gap is **stationary cast recovery → immediate strafe/reversal without dodge**. Generic action framing still releases at decay 9 in that path, so residual target-side framing may compete with movement look-ahead after a direct reversal. Reproduce and measure before changing it.

## Bridge safety

This file is intentionally a new pending bridge entry because the available connector does not provide an atomic safe append primitive for the shared bridge history. No existing bridge history was erased, replaced or rewritten.
