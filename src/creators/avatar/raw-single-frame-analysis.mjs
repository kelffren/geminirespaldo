/* KELO-INDEX
 * area: CREATORS / AVATAR / RAW SINGLE FRAME
 * owner: deterministic fallback analysis for arbitrary AI images
 * keys: RAW IMAGE SINGLE FRAME FALLBACK FULL PIXELS ASPECT RATIO MANUAL LAYOUT
 * purpose: preserve the entire source image as one intentional frame even when automatic spritesheet interpretation fails
 * public-api: analyzeRawSingleFrameAsset
 * do-not: infer animation, remove backgrounds, invent pixels, mutate source image or bypass final review
 */
import{analyzeSpriteForeground,connectedSpriteComponents}from'../sprite-compiler/sprite-foreground-analysis.mjs';
import{buildManualSpriteLayout}from'../sprite-compiler/sprite-layout-interpreter.mjs';
import{interpretSpriteRig}from'../sprite-compiler/sprite-rig-interpreter.mjs';
const F=Object.freeze,MAX_BYTES=12*1024*1024,MAX_DIMENSION=4096;
function canvasFor(root,w,h){const c=root.document?.createElement?.('canvas');if(!c)throw new Error('RAW_IMAGE_CANVAS_UNAVAILABLE');c.width=w;c.height=h;return c}
async function bitmapFor(file,root){if(root.createImageBitmap)return root.createImageBitmap(file);const url=root.URL?.createObjectURL?.(file);if(!url)throw new Error('RAW_IMAGE_DECODE_UNAVAILABLE');try{return await new Promise((resolve,reject)=>{const image=new root.Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error('RAW_IMAGE_INVALID'));image.src=url})}finally{root.URL.revokeObjectURL(url)}}
function rawForeground(data,width,height){const base=analyzeSpriteForeground(data,width,height),cleanedData=new Uint8ClampedArray(data),mask=new Uint8Array(width*height);let foregroundPixels=0,transparentPixels=0;for(let p=0;p<mask.length;p++){const a=cleanedData[p*4+3];if(a>16){mask[p]=1;foregroundPixels++}if(a<250)transparentPixels++}const components=connectedSpriteComponents(mask,width,height,{minArea:2});return F({...base,cleanedData,mask,components,contentBounds:F({x:0,y:0,w:width,h:height}),foregroundPixels,foregroundRatio:foregroundPixels/Math.max(1,width*height),removedPixels:0,softenedPixels:0,transparency:transparentPixels/Math.max(1,width*height),rawPixelsPreserved:true})}
function intentionalFullCanvasLayout(foreground,width,height){const base=buildManualSpriteLayout(foreground,{columns:1,rows:1,sourceRects:[{x:0,y:0,w:width,h:height}]}),frame=base.groups?.[0]?.[0]||{};const rawFrame=F({...frame,cell:F({x:0,y:0,w:width,h:height}),sourceRect:F({x:0,y:0,w:width,h:height}),bounds:F({x:0,y:0,w:width,h:height}),area:Math.max(1,foreground.foregroundPixels),empty:false,boundaryPixels:0,boundaryRatio:0,touchesCanvasEdge:false,cx:width/2,cy:height/2,feetY:height}),groups=F([F([rawFrame])]),evidence=F({...base.evidence,frameCount:1,detectedFrames:1,emptyFrames:0,falseFrames:0,internalEmptySlots:0,coverage:1,missedForeground:0,nonEmpty:1,sizeConsistency:1,heightConsistency:1,widthConsistency:1,clipping:0,canvasClipping:0,groupConsistency:1,alignment:1,countAgreement:1,componentPurity:1,boundaryTouches:0,separators:1});return F({...base,mode:'raw-single-frame',groups,frames:F([rawFrame]),rows:1,columns:1,frameCounts:F([1]),evidence,score:1,why:'explicit user intent: preserve the full source canvas as one frame',signature:'raw-single-frame:1x1'})}
export async function analyzeRawSingleFrameAsset(file,{root=globalThis,onProgress=null}={}){
 if(!file)throw new Error('RAW_IMAGE_FILE_REQUIRED');if(Number(file.size)>MAX_BYTES)throw new Error('RAW_IMAGE_FILE_TOO_LARGE');if(file.type&&!/^image\/(png|webp|jpeg)$/.test(file.type))throw new Error('RAW_IMAGE_TYPE_UNSUPPORTED');
 onProgress?.({stage:'raw-decode',message:'RAW 1 FRAME · leyendo imagen completa…'});const bitmap=await bitmapFor(file,root),width=bitmap.width||bitmap.naturalWidth,height=bitmap.height||bitmap.naturalHeight;if(!width||!height)throw new Error('RAW_IMAGE_INVALID');if(width>MAX_DIMENSION||height>MAX_DIMENSION){bitmap.close?.();throw new Error(`RAW_IMAGE_DIMENSIONS_TOO_LARGE:${width}x${height}`)}const canvas=canvasFor(root,width,height),ctx=canvas.getContext('2d',{willReadFrequently:true});ctx.clearRect(0,0,width,height);ctx.drawImage(bitmap,0,0);bitmap.close?.();const data=ctx.getImageData(0,0,width,height).data;
 onProgress?.({stage:'raw-foreground',message:'RAW 1 FRAME · preservando todos los píxeles y el aspect ratio…'});const foreground=rawForeground(data,width,height),layout=intentionalFullCanvasLayout(foreground,width,height),layoutReport=F({version:'raw-single-frame-layout-v1.1',foreground,best:layout,hypotheses:F([layout]),alternatives:F([]),confidence:1,margin:1,reviewRequired:false,reviewReasons:F([]),rawIntent:true}),rigReport=interpretSpriteRig(layout,{rigHint:1,sourceDirectionOrder:['s']});
 return F({version:'kelo-raw-single-frame-analysis-v1.1.0',compilerVersion:'6.1.0',width,height,columns:1,rows:1,sourceColumns:1,sourceRows:1,frameCounts:F([1]),detectedFrames:1,directionKeys:F(['s']),directions:1,rigProfileId:rigReport.best?.profile||'sprite-rig-1d',rowMap:rigReport.best?.rowMap||F({s:0}),directionConfidence:1,directionMode:'raw-single-frame',detectionMode:'manual:raw-single-frame',strategy:'raw-single-frame',confidenceScore:1,autoCrop:false,removeBackground:false,backgroundKind:foreground.background.kind,backgroundRgb:foreground.background.rgb,backgroundThreshold:foreground.background.coreThreshold,backgroundUniform:foreground.background.uniform,contentBounds:F({x:0,y:0,w:width,h:height}),sourceRects:F([{x:0,y:0,w:width,h:height}]),hypotheses:F([]),rigHypotheses:F([]),reviewRequired:true,reviewReasons:F(['RAW_IMAGE_REVIEW_REQUIRED']),status:'REVIEW_REQUIRED',frameMs:140,universalAuto:false,canonicalRig:true,scaleLock:false,deterministicOnly:true,intentionalRawFrame:true,_foreground:foreground,_layoutReport:layoutReport,_rigReport:rigReport,_fileName:String(file.name||'')});
}
export const __rawSingleFrameInternals=F({rawForeground,intentionalFullCanvasLayout});
