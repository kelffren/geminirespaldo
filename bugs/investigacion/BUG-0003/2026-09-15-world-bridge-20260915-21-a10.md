BUG: BUG-0003
FECHA: 2026-09-15
VERSION / BUILD: world-bridge-20260915-21 / A10
ESTADO: vigente
ENTORNO: local free-path Chromium iPhone UA · target LIVE iPhone Safari

# A10 — endurecer A9 sin reescribir Studio

## Causa raíz (localmente discriminada)
1. Barrel estático `register-core-tools` + `importCurrent`/optional tools a los ~4s mataban el event loop post-chrome.
2. `markProvisionalControls` dejaba TODOS los botones `disabled` tras quitar loading (TREE/tools no respondían).
3. Semilla TREE sin prefabs → `STUDIO_PREFAB_UNKNOWN` silencioso.

## Cambios
- `register-core-tools-serial.mjs` en iPhone
- Skip `importCurrent` automático en phone (documento vacío editable)
- Semilla TREE + `ensurePhonePlacementPrefabs`
- Re-enable provisional controls
- Optional/extras a 12s/18s; productivity panel a 14s
- Retry estable `…-21-retry` (sin nonce)
- Build `world-bridge-20260915-21`

## Verificación local (NO iPhone)
- smoke: open + tools 6s + TREE placement + reopen PASS
- audits world-editor-* PASS

## Gate real
iPhone Safari Create→World: tools 10s, colocar TREE, mover, reopen.
