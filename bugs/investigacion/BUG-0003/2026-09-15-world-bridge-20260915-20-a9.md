BUG: BUG-0003
FECHA: 2026-09-15
VERSION / BUILD: world-bridge-20260915-20 / A9
ESTADO: vigente
ENTORNO: LIVE iPhone Safari · Create -> World

# A9 — muerte post-chrome, no el Hub

## Hecho
El jugador sigue reportando freeze con LIVE en `world-bridge-20260915-19`.
A8 ya pintaba tools y saltaba el overlay. El tab muere *después*.

## Causas que A1–A8 no cerraron
1. `studio-entry.mjs` tenía 15 imports estáticos: Safari parsea kernel+tools+compiler+IDB+overlay renderer como un solo grafo cuando el chrome ya está en pantalla.
2. `createStudioLiveShell` volvía a hacer innerHTML del árbol completo.
3. `cloneNode(true)` del shell duplicaba el chrome en memoria justo antes del hydrate.
4. `importCurrent` hacía structuredClone del snapshot a los 200 ms.
5. `studio-boot-extras.mjs` (22 módulos) a los 2.8 s.
6. Launcher con nonce único por tap (H6 / MDN module identity leak).
7. Cache-bust desalineado: launcher `20260914-3`, entry `20260914-13`, workspace `20260915-19`.

## A9 (sin reescribir Studio)
- studio-entry: imports dinámicos serializados con yield; iPhone no carga overlay renderer ni worker.
- Shell: reuse del DOM pintado + AbortController; no innerHTML ni cloneNode.
- importCurrent a los 8 s en iPhone; extras a los 15 s; thumbs de assets solo al tocar EDIT.
- Launcher/Hub/entry/workspace/bridge en `world-bridge-20260915-20` sin nonce.

## Gate
Solo iPhone real. Tools visibles 10 s y reopen sin black tab.
No VERIFIED desde headless.
