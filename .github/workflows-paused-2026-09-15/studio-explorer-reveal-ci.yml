name: Studio Explorer Reveal CI

on:
  push:
    branches: [main]
    paths:
      - 'src/studio/input/studio-explorer-reveal-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-explorer-reveal-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-explorer-reveal-ci.yml'
  pull_request:
    paths:
      - 'src/studio/input/studio-explorer-reveal-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-explorer-reveal-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-explorer-reveal-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  explorer-reveal-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax
        run: |
          node --check src/studio/input/studio-explorer-reveal-controller.mjs
          node --check src/studio/studio-entry.mjs
          node --check scripts/studio-explorer-reveal-audit.mjs
      - name: Explorer reveal contract
        run: npm run audit:studio:explorer-reveal
