name: Studio Snap Cycle CI

on:
  push:
    branches: [main]
    paths:
      - 'src/studio/input/studio-snap-cycle-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-snap-cycle-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-snap-cycle-ci.yml'
  pull_request:
    paths:
      - 'src/studio/input/studio-snap-cycle-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-snap-cycle-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-snap-cycle-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snap-cycle-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax
        run: |
          node --check src/studio/input/studio-snap-cycle-controller.mjs
          node --check src/studio/studio-entry.mjs
          node --check scripts/studio-snap-cycle-audit.mjs
      - name: Snap cycle contract
        run: npm run audit:studio:snap-cycle
