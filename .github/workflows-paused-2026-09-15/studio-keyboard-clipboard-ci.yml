name: Studio Keyboard Clipboard CI

on:
  push:
    branches: [main]
    paths:
      - 'src/studio/input/studio-keyboard-clipboard-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-keyboard-clipboard-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-keyboard-clipboard-ci.yml'
  pull_request:
    paths:
      - 'src/studio/input/studio-keyboard-clipboard-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-keyboard-clipboard-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-keyboard-clipboard-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  keyboard-clipboard-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax
        run: |
          node --check src/studio/input/studio-keyboard-clipboard-controller.mjs
          node --check scripts/studio-keyboard-clipboard-audit.mjs
          node --check src/studio/studio-entry.mjs
      - name: Keyboard clipboard contract
        run: npm run audit:studio:keyboard-clipboard
