name: Studio Keyboard Duplicate CI

on:
  push:
    branches: [main]
    paths:
      - 'src/studio/input/studio-keyboard-duplicate-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-keyboard-duplicate-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-keyboard-duplicate-ci.yml'
  pull_request:
    paths:
      - 'src/studio/input/studio-keyboard-duplicate-controller.mjs'
      - 'src/studio/studio-entry.mjs'
      - 'scripts/studio-keyboard-duplicate-audit.mjs'
      - 'package.json'
      - '.github/workflows/studio-keyboard-duplicate-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  keyboard-duplicate-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax
        run: |
          node --check src/studio/input/studio-keyboard-duplicate-controller.mjs
          node --check src/studio/studio-entry.mjs
          node --check scripts/studio-keyboard-duplicate-audit.mjs
      - name: Keyboard duplicate contract
        run: npm run audit:studio:keyboard-duplicate
